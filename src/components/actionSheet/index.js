import HiActionSheet from './HiActionSheet.vue';

/* istanbul ignore next */
HiActionSheet.install = function(Vue) {
  Vue.component(HiActionSheet.name, HiActionSheet);
};

export default HiActionSheet;
